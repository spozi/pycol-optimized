# Dependencies for the experiments only. The library in ../src needs just
# numpy and torch; everything below is for BERT and the evaluation metrics.
#
# On a GPU server, install torch first from the index matching the CUDA
# version, then this file:
#
#   python -m pip install "torch==2.13.0" \
#       --index-url https://download.pytorch.org/whl/cu132
#   pip install -r requirements.txt
#   pip install -e ..

torch>=2.13,<3
numpy>=2.2,<3
transformers>=4.44
scikit-learn>=1.5
