# Derived artifacts: embeddings, downloaded corpora, and run outputs.
# Regenerated by `python -m complexity_augmentation.run`.
cache/
results*/
__pycache__/

# Run logs. Note that `results*/` above matches directories only, so the
# nohup logs sitting beside them need their own rule.
*.log
